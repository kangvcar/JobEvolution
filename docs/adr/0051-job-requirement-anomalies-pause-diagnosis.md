# 岗位要求异常只暂停当前岗位

诊断发布使用要求等价数检查岗位级噪声:独立要求边计 1,要求组按最低满足数量计数。必备超过 12、全部正式要求超过 24,或相对上一版本的新增必备超过 `max(3,上期必备要求等价数 × 50%)`、新增正式要求超过 `max(5,上期正式要求等价数 × 50%)` 时,系统暂停该岗位的推荐与诊断。管理员可以修正后重跑,也可以留下理由后放行；其他岗位不受影响。该规则在数据可信度和岗位差异之间留了一条人工出口,同时阻止异常批次直接生成职业结论。
