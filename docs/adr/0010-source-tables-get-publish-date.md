# 缺日期的原始表补「发布日期」列

智联和 IT 表已有完整日期。综合招聘 2026 只有 `publish_detail` 里的「03-14发布」；软件 2026、互联网 2024 没有日期列。为了表能单独打开、采集能直接读到日期，给这三张表加「发布日期」：年取自文件名，月日优先从「03-14发布」取，没有则用 fingerprint 稳定哈希。原列不改。`publish_detail` 仍保留抓取原文。
