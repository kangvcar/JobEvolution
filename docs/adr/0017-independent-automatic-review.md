# 自动审核不得由抽取模型自批

产品只保留一个默认关闭的全局自动审核开关。开启后也只有高置信岗位事实可以尝试自动通过:先检查摘录确实存在于证据、独立源与覆盖率达标、字段和时间合法,再交给独立于抽取模型的审核模型复核;两段都通过才写 `auto_passed`,并记录模型、提示词版本、理由与校验结果。中置信事实留在预览层,低置信事实留在待审队列,不能被开关放行。

自动审核模型超时、报错或不可用时失败关闭,相关提案留在待审队列;故障不阻止同批已由人工批准且通过确定性校验的事实发布。
