# 官网 JD 快照身份含正文指纹

同一招聘门户的同一站点 job_id 在职责或要求改写后必须留下新的 JD 快照,旧快照继续作证据。现有 `sha256(source + job_id)` 会把新旧正文压进同一个文件名并在 Redis `ingest:fp` 上永久跳过。官网增量因此把快照 id 改为 `sha256(source + job_id + body_simhash)` 的前 16 位,用「上次正文指纹」判断要不要写入。独立源仍按规范化公司名计,不按快照条数计。覆盖原文件会毁掉双时间,另建版本目录没有现成读取路径。
