# 管理端继续使用共享口令

长期产品的受控管理端继续使用一个共享管理员口令,不引入个人管理员身份或角色系统。审核记录只能证明持有管理员口令的一方在某时作出决定,不能归责到具体个人;自动审核开关和事实撤回同样由该口令控制。若出现多人责任追踪、最小权限或口令轮换无法满足的要求,再用个人身份替换这条边界。

浏览器只把口令提交给登录接口,成功后使用 Redis 中的短期管理会话和 `Secure`、`HttpOnly`、`SameSite=Strict` Cookie;写操作校验 CSRF,登录限速。前端不得把原始口令放进 Cookie、localStorage 或后续 API 请求。
