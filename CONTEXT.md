# JobEvolution

多源数据驱动的岗位能力图谱产品(XH-202621):从招聘数据流中发现新岗位、追踪既有岗位能力演化,为求职者提供图谱定位、差距分析与学习路径。

## Language

### 图谱本体

**领域**:
新一代信息技术的四个固定顶层分区:人工智能、大数据、智能系统、物联网。
_Avoid_: 行业、赛道

**岗位**:
劳动力市场中一类职位的规范化表示,挂中国职业分类大典编码与 ESCO/O\*NET 外部 ID 映射;初级/高级不分裂节点,级别差异表达在要求边上。
_Avoid_: 职位、职业(仅指大典的官方分类条目时可用"职业")

**覆盖靶子**:
用来把 JD 标题对齐到岗位节点的规范岗位名（产品篇那 17 个）。是对齐用的名字，不是必须入谱、必须萌芽、必须成型的计数名单。
_Avoid_: 种子岗位、验收名单、手录名单

**技能类目**:
技能点的聚合层,承担全景图谱的"技术栈视图"与导航收纳,不参与差距分析对账。一期固定五桶:语言、框架、平台、工程、领域知识。
_Avoid_: 能力域、技能分类

**技能点**:
图谱最小颗粒,差距分析的对账单位(如 FastAPI、Cypher);赛题语境中的"能力项"即技能点。
_Avoid_: 能力项、技能标签

**要求边**:
岗位指向技能点的核心边,携带:必备/加分、熟练级、权重、有效期(valid_from/valid_to)、置信度、来源列表、适用级别(levels)。
_Avoid_: 技能要求关系、需求边

**熟练级**:
要求边上的三档刻度:了解、熟练、精通。
_Avoid_: 1–6 级、掌握程度

**证据**:
支撑图谱事实的原始数据引用(JD 快照、数据源记录),每条要求边和演化事件都可溯源到证据。
_Avoid_: 数据源引用、episode

### 演化与生命周期

**双时间**:
观察时间(何时在数据源看到)与有效期(能力要求何时真实生效/失效)分开记录;旧要求边写失效时间而非删除。
_Avoid_: 时间戳(不加限定时)

**演化事件**:
图谱变更的原子记录,分边级(技能要求新增/移除/修改)与节点级(新岗位入谱/岗位定义更新/岗位状态流转);携带发生时间、证据引用、置信度、审核状态。产品形态是切片差分与发现页流水,不是时间轴控件。
_Avoid_: 变更记录、更新日志

**岗位状态**:
新岗位生命周期:候选(未入谱;发现页可展示漏斗,不可诊断、不可当正式岗)→ 萌芽(入谱,标"新兴":≥3 独立源、90 天窗、LLM 判定为新岗位)→ 成型:(≥10 独立源 或 持续 ≥6 个月) 并且 岗位定义曾获批准或自动通过。阈值是可配置常量。已判别名不占候选列,注记在被并入岗上;源节点可留着挂 `ALIAS_OF`,discover 按出边过滤。状态由独立源从证据计票,用 `Evidence.observed_at` 算 90 天窗与持续月数,不手写、不靠现场等满。萌芽与成型的个数随证据变，不是验收计数。赛题那一对与「LLM 业务工程师」的 ALIAS_OF 须由管线从 JD 快照产出。
_Avoid_: 岗位阶段

**独立源**:
去重后的公司主体,用规范化公司名计票;ATS / 高校站是渠道(`Evidence.source`),不是独立源。正文 simhash 近似重复的 JD 只保留时间最早的一条作证据,其余不计票。
_Avoid_: 数据源数量、JD 条数(未去重时)

**簇内覆盖率**:
某技能点在该岗位去重 JD 簇中出现的比例;≥30% 才进入必备/加分池,低于此为观测中。
_Avoid_: 支持率、提及率

**观测中**:
已从 JD 抽出但未达覆盖率门槛的技能点,不写入要求边,只作发现信号。求职者文案写「市场开始提,还没进要求,不算缺口」;对账表仍用本词。
_Avoid_: 候选技能

**置信层**:
要求边的三级,按优先级:无证据链 → 低;≥3 独立源且抽取置信 ≥0.8 → 高;抽取置信 ≥0.5 → 中;其余 → 低。高：直通开着时可自动过,关着也进待审;中待审,直通开着时入谱但标「待更多证据」;低永不自动入谱,管理员仍可批准。
_Avoid_: 置信度分数(分数是边上的数;层是产品行为)

### 审核

**待审队列**:
LLM 生成的岗位定义与能力变更在入谱前的暂存区,由管理员批准/修改/驳回;是幻觉防控的最后一道闸。既有岗位的能力更新与新岗位发现共用同一套闸。
_Avoid_: 审核列表

**直通开关**:
全局开关,开启后仅跳过中/高置信层的人工批准,演化事件记「自动通过」;低置信层的证据底线不跳过。
_Avoid_: 跳过审核模式

### 匹配与评测

**技能对齐**:
简历或 JD 中的技能表述对上技能点的两步:同义词表精确匹配优先,未命中再用嵌入余弦(默认阈值 0.85,与图谱实体消解同一套);仍未命中记为缺口。评测时阈值冻结。
_Avoid_: 模糊匹配、语义匹配(不加限定时)

**匹配分**:
0–100 的技能覆盖分:`100 × (必备覆盖 + 0.3×加分覆盖) / (必备满分 + 0.3×加分满分)`。边上的 `weight` 不进分,当 1。分母为 0 则分数 0、档位不匹配。必备未覆盖记 0;简历标了熟练级且低于岗位要求记 0.5(半档);简历没标熟练级则只比有无。加分缺失不伤必备分。经验年限与学历是并列展示维,不进此分。
_Avoid_: 匹配度、相似度、综合得分

**缺口集**:
目标岗位的必备技能点中,简历经技能对齐后未覆盖(含半档不足)的集合;差距分析与匹配评测的对账对象。半档只在简历金标带了熟练级时计入。
_Avoid_: 缺失技能列表、gap

**切片差分**:
当前岗切片上,标出本周期相对上一周期的要求边变化:新增或升值、已写 valid_to。点节点仍开证据。按序重演演化事件是实现,不是页面控件。
_Avoid_: 时间轴、回放、diff 图

**换档条件**:
把缺口与半档补到岗位要求后,档位能升一档的最小技能点集合。学习路径按此排序,不是按缺口出现顺序取前 5。
_Avoid_: 提升建议、必修课

**对照链接**:
带 session_id 与 job_id 的诊断 URL,TTL 跟简历会话。不注册。过期回 idle。
_Avoid_: 分享报告、历史诊断

**三项准确率**:
赛题达标口径均为技能点集合的 set-based F1 ≥0.90。JD 解析:抽出技能点 vs 金标 JD 技能点(≥100 条)。简历提取:抽出技能点 vs 金标简历技能点(100 份,可加到 200;字段级 F1 另报不进三项)。匹配:金标简历技能 × 金标岗位要求上的缺口集 F1(≥100 对),不喂解析输出。学习路径不定准确率,抽检换档条件上的技能点是否都有资源建议。
_Avoid_: 匹配准确率(未指明缺口集 F1 时)
